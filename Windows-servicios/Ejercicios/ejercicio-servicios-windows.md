# ejercicio de servicios

Configuar el servicio de actualizaciones de Windows para que se inicie
automáticamente con el arranque del OS, reiniciar el sistema
y comprobar que el servicio está arrancado

Además, configurar que ante el primer fallo del servicio, el servicio
se reinicie, pero ante un segundo fallo el sistema se reinicie.
Para comprobar que está bien configurado, abrir el task manager y forzar 
el fin del servioc manualmente (2 veces)